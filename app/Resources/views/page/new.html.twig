{% extends 'base.html.twig' %}

{% block body %}
<div class="well">
    <h1>Nouvelle page</h1>
</div>
<div class="container-fluid">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer la page" class="btn btn-primary" />
    {{ form_end(form) }}
	
</div>
<div class="container-fluid">
    <ul class="list-unstyled ">
        <li>
            <a href="{{ path('admin_page_index') }}" class="btn btn-info">Retour à la liste</a>
        </li>
    </ul>
</div>
{% endblock %}
